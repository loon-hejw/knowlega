# Deprecated: the standalone Knowledge Core runtime configuration is no longer
# a product entrypoint. Configure the merged QM backend from:
#
#   configs/qm-config.example.yaml
#
# Create the ignored local file with:
#
#   cp configs/qm-config.example.yaml configs/qm-config.yaml
#
# Then start:
#
#   go run ./cmd/qm-backend --config configs/qm-config.yaml
